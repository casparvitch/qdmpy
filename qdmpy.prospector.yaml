

ignore-paths:
  - docs
  - examples
  - other
  - build
  - dist

doc-warnings: false

pyroma:
  run: true

vulture:
  run: true

bandit:
  run: true

mccabe:
  run: false

# run (from dir with .git) via: 
# `prospector --profile qdmpy.prospector.yaml -o grouped:prospector.log`